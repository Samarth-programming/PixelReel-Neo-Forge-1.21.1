package com.pixelreel.client.gui.ondemand;

/** Screen that shows on-demand browse pages and wants to be told when a new page lands. */
public interface BrowseResultListener {
	void onBrowseUpdated();
}
